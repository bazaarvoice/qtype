# Test spec for VectorIndex.embedding_model reference resolution

id: test_vector_index_embedding_model

models:
  - type: EmbeddingModel
    id: test_embedding_model
    provider: openai
    model_id: text-embedding-ada-002
    dimensions: 1536

indexes:
  - type: VectorIndex
    id: test_index
    name: test-index
    embedding_model: test_embedding_model  # String should resolve to Reference[EmbeddingModel]

flows:
  - id: test_flow
    steps:
      - id: test_step
        type: PromptTemplate
        template: "test"
