# Test spec for Step.outputs reference resolution

id: test_step_outputs

flows:
  - id: test_flow
    variables:
      - id: output_var
        type: text
    steps:
      - id: test_step
        type: PromptTemplate
        template: "Hello"
        outputs:
          - output_var  # String should resolve to Reference[Variable]
