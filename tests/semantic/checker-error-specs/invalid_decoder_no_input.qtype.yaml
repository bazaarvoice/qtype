# Decoder must have exactly one text input - no inputs
id: test_app
flows:
  - id: test_flow
    variables:
      - id: result
        type: int
    outputs:
      - result
    steps:
      - id: decoder1
        type: Decoder
        format: json
        inputs: []
        outputs:
          - result
