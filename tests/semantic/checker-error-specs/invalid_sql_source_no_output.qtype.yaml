# SQLSource must have at least one output - no outputs
id: test_app
flows:
  - id: test_flow
    steps:
      - id: sql1
        type: SQLSource
        connection: "sqlite:///test.db"
        query: "SELECT * FROM users"
        outputs: []
