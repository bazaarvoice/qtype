# VectorSearch must have exactly one output of type List[RAGSearchResult] - wrong element type
id: test_app
models:
  - id: embed_model
    type: EmbeddingModel
    provider: openai
    dimensions: 768
indexes:
  - id: test_index
    type: VectorIndex
    name: test_vector_index
    module: qdrant.QdrantVectorStore
    embedding_model: embed_model
flows:
  - id: test_flow
    variables:
      - id: query_text
        type: text
      - id: result
        type: list[RAGChunk]
    inputs:
      - query_text
    outputs:
      - result
    steps:
      - id: search1
        type: VectorSearch
        index: test_index
        inputs:
          - query_text
        outputs:
          - result