# REMOVED: Conditional branching has been removed from the DSL as per dsl_changes.md #8
# This file is kept for reference but the test should be disabled or removed
id: app_condition_then_ref_removed
description: Conditions are no longer supported in the DSL
flows:
  - type: Flow
    id: f1
    variables:
      - id: s1_prompt
        type: text
      - id: result
        type: text
    steps:
      - id: s2
        type: PromptTemplate
        template: bye
        outputs:
          - result
