id: app_flow_steps_ref
flows:
  - type: Flow
    id: f2
    outputs:
      - id: o1
        type: text
    steps:
      - id: s1
        type: PromptTemplate
        template: hi
        outputs:
          - prompt
      - id: s2
        type: Decoder
        format: json
        inputs:
          - prompt
        outputs:
          - o1
  - type: Flow
    id: f1
    steps:
      - $ref: s1
      - $ref: s2
