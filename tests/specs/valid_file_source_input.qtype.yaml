id: file_source_input_test
description: Test FileSource with path from input variable
flows:
  - type: Flow
    id: read_file_flow
    outputs:
      - id: content
        type: text
    steps:
      - id: read_data
        type: FileSource
        path: s3://my-bucket/data.json
        outputs:
          - content
