id: invalid_file_source_test
description: Test FileSource with no path field or input variable - should fail validation
flows:
  - type: Flow
    id: read_file_flow
    outputs:
      - id: content
        type: text
    steps:
      - id: read_data
        type: FileSource
        outputs:
          - content
