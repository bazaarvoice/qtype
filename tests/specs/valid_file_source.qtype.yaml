id: file_source_test
description: Test FileSource with direct path specification
flows:
  - type: Flow
    id: read_file_flow
    variables:
      - id: file_content
        type: text
    outputs:
      - file_content
    steps:
      - id: read_data
        type: FileSource
        path: 
          uri: s3://my-bucket/data.json
        outputs:
          - file_content
